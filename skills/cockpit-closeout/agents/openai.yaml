interface:
  display_name: "Cockpit 阶段收束"
  short_description: "核对阶段 delta、完成本地收束，并在条件具备时可选登记 Cockpit 检查点"
  default_prompt: "使用 $cockpit-closeout 核对当前阶段并完成本地收束与可选检查点登记。"
policy:
  allow_implicit_invocation: false
