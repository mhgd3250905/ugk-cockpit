interface:
  display_name: "Cockpit 交接"
  short_description: "整理阶段事实、生成可验证交接并安全结束 UGK Cockpit 会话"
  default_prompt: "使用 $cockpit-handoff 整理本阶段事实并生成交接。"
dependencies:
  tools:
    - type: "mcp"
      value: "ugk-cockpit"
      description: "UGK Cockpit 本地 MCP"
policy:
  allow_implicit_invocation: true
