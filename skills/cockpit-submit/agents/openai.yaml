interface:
  display_name: "Cockpit 送审"
  short_description: "保存开发空间成果并安全送交主项目审核"
  default_prompt: "使用 $cockpit-submit 保存当前开发空间成果并送交主项目审核。"
dependencies:
  tools:
    - type: "mcp"
      value: "ugk-cockpit"
      description: "UGK Cockpit 本地 MCP"
policy:
  allow_implicit_invocation: false
