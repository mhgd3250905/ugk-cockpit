interface:
  display_name: "发布工作说明"
  short_description: "向所属主项目发布一条工作说明待办；不强绑代码保存上传，不假设任务已完成"
  default_prompt: "使用 $cockpit-submit 向当前项目发布工作说明，说明中整理当前进展与待办。"
dependencies:
  tools:
    - type: "mcp"
      value: "ugk-cockpit"
      description: "UGK Cockpit 本地 MCP"
policy:
  allow_implicit_invocation: false
