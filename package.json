{
  "name": "ugk-cockpit",
  "version": "0.1.0-alpha.4",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.15.x"
  },
  "scripts": {
    "serve": "npm run build:web && node src/main.mjs",
    "dev:web": "vite",
    "build:web": "vite build",
    "test": "node --test --test-concurrency=1",
    "test:phase0": "node --test --test-concurrency=1 test/phase0/*.test.mjs"
  },
  "dependencies": {
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  }
}
