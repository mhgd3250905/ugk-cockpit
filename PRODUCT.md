# UGK Cockpit 产品基线规范

- **文档语境 (Register)**: `product`
- **产品版本**: `0.1.0-alpha`
- **设计定位**: 本机优先的个人 AI 开发控制台 (Local-First AI Development Mission Control Product UI)

---

## 1. 产品定位与目标用户

UGK Cockpit 是专门为**项目所有者 (Project Owner)**、独立开发者与技术负责人打造的本机工作控制台。

### 目标用户特征
- **日常场景**：同时推进多个 Git 项目，把具体的代码实现、修复和重构任务派发给多个 AI Agent（如 Codex、ZCode、Antigravity 等）。
- **用户心智**：用户是项目的主人和决策者，不是只查看 Git 底层日志的运维人员。用户需要清楚掌握“谁在动哪份代码、当前到了哪一步、有什么需要决策”，而不需要在日常操作中记忆和输入底层 Git 命令、worktree 路径、JSON 结构体或内部 UUID。
- **界面体验**：系统内部保持严谨完备的状态机与持久化日志，但面向用户的普通界面必须使用清晰、直观、可理解、可撤销、可继续的自然人话。

---

## 2. 核心价值主张：晨间工作简报与 10 秒行动法则

UGK Cockpit 的核心页面是**晨间工作简报 (Morning Briefing)**，而不是冷冰冰的工程监控大盘。

### 10 秒行动法则 (10-Second Triage Rule)
用户打开控制台后，必须在 **10 秒内** 明确获得三项核心答案：
1. **哪个项目最需要我处理？** —— 界面按行动状态统一分组呈现所有项目，卡片背景与状态边框直观区分待处理项，每个项目只显示一次。
2. **为什么？** —— 使用人话清楚说明原因（例如：“开始前已经有本地改动”、“任务已准备好，等待 AI 接手”、“AI 已经接手这项任务”）。
3. **我现在按哪个按钮？** —— 有真实后端动作时提供单一、直接的主操作入口（例如：“查看并继续”、“交给 AI”）；只有风险说明时展示只读引导，不伪造按钮。

---

## 3. 本机安全边界与不变式 (Safety Boundaries & Invariants)

Cockpit 运行在用户本地设备，恪守严格防御与安全边界：

### 默认保守操作
- **不自动清理**：绝不自动清理未保存的本地修改；已有改动一律标记为“开始前已有改动”并原样保留。
- **不破坏性操作**：绝不未经确认执行 `checkout`、`merge`、`reset --hard`、`clean -f` 或覆盖删除用户代码。
- **不私自提交或上传**：绝不静默提交 Git commit，绝不向外部未授权网络上传或同步业务代码。

### 显式授权与路径隔离
- **手动单选授权**：通过置顶的 Windows 原生系统选择器逐个添加项目，一次只处理用户明确选中的文件夹，严禁后台静默扫描用户磁盘或自动导入项目。
- **符号链接与逃逸防护**：严格防范 junction / symlink 跨界逃逸与虚假身份重绑；代码位置身份变化时强制要求用户重新确认。

### 单一写入租约与会话安全
- **单写入租约 (Single Write Lease)**：同一工作副本同一时间最多仅允许一个活跃写入会话；遇到并发编辑时默认只读。
- **接管二次确认 (Explicit Takeover)**：接管尚未交接的旧会话必须经过用户显式二次确认，绝不发生静默抢占。
- **修改归属确定性**：无法确凿证明修改归属的代码变动必须标记为 `unattributed`（未归属改动），严禁猜测 Agent 身份。

### 状态持久化与崩溃防护
- **零幽灵完成**：操作持久化边界异常时绝不显示虚假成功；任何离线或陈旧数据必须标注最后更新时间。
- **异常三要素**：所有系统与业务异常必须完整说明：
  1. **发生了什么**；
  2. **代码是否受影响**；
  3. **推荐下一步动作是什么**。

---

## 4. 产品性格与设计调性 (Persona & Tone)

| 特质 | 说明 | 界面体现 |
| :--- | :--- | :--- |
| **专业 (Professional)** | 沉稳精炼，尊重工程师决策，聚焦真实工作流与客观事实。 | 界面排版井然有序，信息层级分明，只呈现决策必需的高价值信息。 |
| **克制 (Restrained)** | 不喧宾夺主，拒绝花哨图表与过度设计，不打扰沉浸式工作。 | 暖中性画布、深墨蓝文字、深橙关键动作；高级技术参数默认折叠在“技术详情”内。 |
| **可信 (Trustworthy)** | 严谨诚实，绝不报喜不报忧，不出现“大概完成”或“猜测已提交”。 | 未持久化确认绝不假称“已保存”；异常明确告知代码安全状态，并提供安全恢复步骤。 |

---

## 5. 负面参考与边界反例 (Anti-References)

为确保产品方向不偏离核心价值，明确以下负面反例：

### 反记事簿 (Anti-Scratchpad / Anti-Notes App)
- **不是散碎便签**：Cockpit 不是记事本、便签纸或随手记 TODO 工具。
- **事实源约束**：所有工作任务、代码基线与接手记录必须锚定在真实的本地 Git 状态与不可变接手手册（Handoff Receipt）上，杜绝脱离代码实际状态的随意涂鸦。

### 反通用 SaaS 监控大盘 (Anti-Generic SaaS Dashboard)
- **拒绝虚荣指标**：不是堆砌花哨饼图、折线图、多层嵌套导航栏或复杂权限审批流的企业后台。
- **拒绝通用模板**：避免使用千篇一律的暗黑蓝紫渐变、闪烁动效与密集表格。Cockpit 采用高信噪比的产品化 Mission Control 布局，专注当下单机开发实战。

---

## 6. 无障碍与可用性基线 (WCAG 2.2 AA Baseline)

- **色彩对比度**：正文文字与背景对比度严格达到 **4.5:1** 以上；大字号标题与主要交互组件对比度达到 **3:1** 以上。
- **状态双重编码**：状态颜色绝不单独承载语义，必须始终与文字标签、状态标题及行动按钮同时呈现。
- **焦点管理**：所有键盘可聚焦元素具备高辨识度的 3px 聚焦轮廓，`:focus-visible` 轮廓偏移 ≥ 3px；模态框打开后约束焦点，关闭后恢复到原触发元素。
- **动效适应**：全面支持 `prefers-reduced-motion`，在用户开启减弱动态效果时将过渡与动画时间缩短至无感，防止视觉眩晕。
- **语义化结构**：使用语义化 HTML5 标签（`main`, `header`, `section`, `article`, `details`）与完备的 ARIA 属性（`role="alert"`, `role="dialog"`, `aria-modal` 等）。

---

## 7. 产品语言与术语对照

普通界面统一采用自然人话，内部技术术语仅在“技术详情”折叠层或代码层出现：

| 内部术语 | 普通界面规范用语 | 说明 |
| :--- | :--- | :--- |
| Repository | 代码位置 | 用户的项目源码所在物理/逻辑目录 |
| Worktree | 工作副本 | 正在编辑或操作的代码工作区 |
| Branch | 工作线 | 当前开发分支 |
| HEAD / Commit | 代码保存点 | Git 提交记录与历史基线 |
| Dirty | 尚未保存到版本记录的本地改动 | 工作区中未提交的修改 |
| Run / Session | AI 工作会话 | Agent 接入并持有写入权限的单次工作阶段 |
| Snapshot | 开始前状态 / 结束时状态 | 阶段开启与收束时的代码快照 |
| HandoffReceipt | 接手记录 / 标准交接手册 | 包含任务进展、下一步建议的结构化交接物 |
| Write lease | 当前有人正在编辑 | 独占写锁与写入授权 |
| Heartbeat | 最近记录时间 | 内部活动记录与写入校验信号，不代表 Agent 在线 |
| Stale | 较早前的活动记录 | 保持 active 状态，不据此推断 AI 中断 |
| Unattributed | 还不知道是谁产生的改动 | 无法确凿证明来源的变动 |
| Foreign change | 工作期间来自其他地方的更新 | 外部进程或用户在会话外的修改 |
